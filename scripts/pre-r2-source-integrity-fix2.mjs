import { readFile, writeFile, rm } from "node:fs/promises";

async function patch(path, transform) {
  const before = await readFile(path, "utf8");
  const after = transform(before);
  if (after === before) throw new Error(`No change produced for ${path}`);
  await writeFile(path, after, "utf8");
}
function once(text, search, replacement, label) {
  const at = text.indexOf(search);
  if (at < 0) throw new Error(`Missing anchor: ${label}`);
  if (text.indexOf(search, at + search.length) >= 0) throw new Error(`Non-unique anchor: ${label}`);
  return text.slice(0, at) + replacement + text.slice(at + search.length);
}

await patch("src/automation/index.ts", (text) => once(text, 'export * from "./planner-output-envelope.ts";\n', "", "stale planner-output-envelope export"));

await patch("src/automation/adapters.ts", (text) => {
  text = once(text, 'import type { EffectivePolicyDecision, RuntimeCapability } from "./effective-policy.ts";\n', 'import type { EffectivePolicyDecision, RuntimeCapability } from "./effective-policy.ts";\nimport type { PlannerProviderRequest } from "./planner-provider-contract.ts";\n', "planner provider request import");
  text = once(text, '  readonly payloadRef: string | null;\n  readonly correlation: ProviderCorrelation;\n}', '  readonly payloadRef: string | null;\n  /** Optional provider-neutral Planner descriptor; never raw prompt/browser state. */\n  readonly plannerRequest?: PlannerProviderRequest | null;\n  readonly correlation: ProviderCorrelation;\n}', "ProviderSubmitInput plannerRequest");
  return text;
});

await patch("src/automation/types.ts", (text) => {
  text = once(text, '  plannerRequestCanonical?: string | null;\n  state: ActionIntentState;', '  plannerRequestCanonical?: string | null;\n  logicalPlannerRequestId?: string | null;\n  plannerRequirementVersionId?: string | null;\n  plannerRequirementPayloadSha256?: string | null;\n  plannerOperation?: "PLAN_REQUIREMENT" | "DETAIL_STAGE" | null;\n  plannerMaxProviderAttempts?: number | null;\n  plannerState?: "ACTIVE" | "PROMOTED" | "FAILED" | null;\n  promotedPlanVersionId?: string | null;\n  state: ActionIntentState;', "ActionIntent logical planner fields");
  text = once(text, '  /** Planner-only logical classification of this concrete provider attempt. */\n  plannerResultClassification?: "INVALID_OUTPUT_RETRYABLE" | null;\n}', '  logicalPlannerRequestId?: string | null;\n  attemptNumber?: number | null;\n  providerTargetRef?: string | null;\n  externalSideEffectCertainty?: ActionOutcomeCertainty | null;\n  /** Planner-only logical classification of this concrete provider attempt. */\n  plannerResultClassification?: "INVALID_OUTPUT_RETRYABLE" | "UNKNOWN_AFTER_SIDE_EFFECT" | "VALID_RESULT" | null;\n}', "ActionAttempt logical planner fields");
  return text;
});

await patch("src/automation/store.ts", (text) => {
  text = once(text, 'import type { PlannerReadyPayload } from "./planner-contract.ts";\n', 'import type { PlannerReadyPayload } from "./planner-contract.ts";\nimport type { NormalizedPlanCandidate } from "./planner-validator.ts";\n', "normalized candidate import");
  text = once(text, '  plannerRequestCanonical?: string | null;\n}\n\nexport interface ActionAttemptInput', '  plannerRequestCanonical?: string | null;\n  logicalPlannerRequestId?: string | null;\n  plannerRequirementVersionId?: string | null;\n  plannerRequirementPayloadSha256?: string | null;\n  plannerOperation?: "PLAN_REQUIREMENT" | "DETAIL_STAGE" | null;\n  plannerMaxProviderAttempts?: number | null;\n}\n\nexport interface ActionAttemptInput', "ActionIntentInput planner fields");
  text = once(text,
    'const item: ActionIntent = { intentId: id(input.intentId, "intentId"), projectId: project.projectId, stageSpecId: input.stageSpecId ?? null, stepSpecId: input.stepSpecId ?? null, attemptId: input.attemptId ?? null, actionType, targetRef, sideEffectClass: input.sideEffectClass, payloadRef, payloadHash, executionOptions, semanticSha256, idempotencyRef, expectedOutcomeRef, policyVersionId, plannerRequestCanonical: optionalText(input.plannerRequestCanonical, "intent.plannerRequestCanonical", 32_768), state: "PLANNED", createdAt: now() };',
    'const intentId = id(input.intentId, "intentId");\n      const plannerOperation = input.plannerOperation ?? (executionOptions.plannerOperation === "PLAN_REQUIREMENT" || executionOptions.plannerOperation === "DETAIL_STAGE" ? executionOptions.plannerOperation : null);\n      const item: ActionIntent = { intentId, projectId: project.projectId, stageSpecId: input.stageSpecId ?? null, stepSpecId: input.stepSpecId ?? null, attemptId: input.attemptId ?? null, actionType, targetRef, sideEffectClass: input.sideEffectClass, payloadRef, payloadHash, executionOptions, semanticSha256, idempotencyRef, expectedOutcomeRef, policyVersionId, plannerRequestCanonical: optionalText(input.plannerRequestCanonical, "intent.plannerRequestCanonical", 32_768), ...(actionType === "PLANNER_REQUEST" ? { logicalPlannerRequestId: input.logicalPlannerRequestId ?? intentId, plannerRequirementVersionId: input.plannerRequirementVersionId ?? (typeof executionOptions.requirementVersionId === "string" ? executionOptions.requirementVersionId : null), plannerRequirementPayloadSha256: input.plannerRequirementPayloadSha256 ?? (typeof executionOptions.requirementPayloadSha256 === "string" ? executionOptions.requirementPayloadSha256 : null), plannerOperation, plannerMaxProviderAttempts: input.plannerMaxProviderAttempts ?? 2, plannerState: "ACTIVE" as const, promotedPlanVersionId: null } : {}), state: "PLANNED", createdAt: now() };',
    "createActionIntent planner fields");
  text = once(text,
    'const item: ActionAttempt = { actionAttemptId: id(input.actionAttemptId, "actionAttemptId"), intentId: intent.intentId, dispatchNumber: previous.length + 1, state: "CREATED", startedAt: null, completedAt: null, executorRef: optionalText(input.executorRef, "actionAttempt.executorRef", 256), recoveryState: "NONE", policyVersionId: intentPin, providerRequestRef: optionalText(input.providerRequestRef, "actionAttempt.providerRequestRef", 256), providerObservationRef: optionalText(input.providerObservationRef, "actionAttempt.providerObservationRef", 256), providerSemanticSha256: optionalText(input.providerSemanticSha256, "actionAttempt.providerSemanticSha256", 128) };',
    'const dispatchNumber = previous.length + 1;\n      const item: ActionAttempt = { actionAttemptId: id(input.actionAttemptId, "actionAttemptId"), intentId: intent.intentId, dispatchNumber, state: "CREATED", startedAt: null, completedAt: null, executorRef: optionalText(input.executorRef, "actionAttempt.executorRef", 256), recoveryState: "NONE", policyVersionId: intentPin, providerRequestRef: optionalText(input.providerRequestRef, "actionAttempt.providerRequestRef", 256), providerObservationRef: optionalText(input.providerObservationRef, "actionAttempt.providerObservationRef", 256), providerSemanticSha256: optionalText(input.providerSemanticSha256, "actionAttempt.providerSemanticSha256", 128), ...(intent.actionType === "PLANNER_REQUEST" ? { logicalPlannerRequestId: intent.logicalPlannerRequestId ?? intent.intentId, attemptNumber: dispatchNumber, providerTargetRef: intent.targetRef, externalSideEffectCertainty: null, plannerResultClassification: null } : {}) };',
    "createActionAttempt planner fields");
  text = once(text, '      const updated: ActionAttempt = { ...attempt, plannerResultClassification: "INVALID_OUTPUT_RETRYABLE" };', '      const updated: ActionAttempt = { ...attempt, externalSideEffectCertainty: receipt.outcomeCertainty, plannerResultClassification: "INVALID_OUTPUT_RETRYABLE" };', "invalid output certainty");

  const marker = '  async createStageSpec(input: StageSpecInput): Promise<StageSpec> {';
  const method = `  async persistValidatedPlannerCandidate(input: { projectId: string; candidate: NormalizedPlanCandidate; actionIntentId: string; actionAttemptId: string; provider: string; providerRequestRef: string; providerObservationRef: string; validationStatus: "VALID" | "VALID_WITH_ASSUMPTIONS" }): Promise<PersistPlannerPlanResult> {\n    return this.transaction((tx) => {\n      const project = tx.require("automationProjects", input.projectId);\n      const intent = tx.require("actionIntents", input.actionIntentId);\n      const attempt = tx.require("actionAttempts", input.actionAttemptId);\n      if (intent.projectId !== project.projectId || intent.actionType !== "PLANNER_REQUEST" || attempt.intentId !== intent.intentId) throw new AutomationStoreError("AUTOMATION_CONFLICT", "Planner promotion correlation is invalid.");\n      const priorPromotion = [...tx.table("auditEvents")].reverse().find((event) => event.eventType === "PLANNER_PLAN_PROMOTED" && event.correlationId === intent.intentId);\n      if (priorPromotion) {\n        const existing = tx.require("planVersions", priorPromotion.entityId);\n        return { planVersion: clone(existing), stageSpecs: clone(tx.table("stageSpecs").filter((item) => item.planVersionId === existing.planVersionId)), stepSpecs: clone(tx.table("stepSpecs").filter((step) => tx.table("stageSpecs").some((stage) => stage.planVersionId === existing.planVersionId && stage.stageSpecId === step.stageSpecId))) };\n      }\n      const c = input.candidate;\n      const requirement = tx.require("requirementVersions", c.requirementVersionId);\n      if (c.projectId !== project.projectId || requirement.projectId !== project.projectId || project.activeRequirementVersionId !== requirement.requirementVersionId || !["CONFIRMED", "ACTIVE"].includes(requirement.status) || c.requirementPayloadSha256 !== requirement.payloadSha256) throw new AutomationStoreError("AUTOMATION_CONFLICT", "Planner candidate does not bind the exact active RequirementVersion.");\n      if (tx.table("planVersions").some((item) => item.planVersionId === c.planVersionId)) throw new AutomationStoreError("AUTOMATION_CONFLICT", "Planner candidate PlanVersion identity already exists without matching promotion evidence.");\n      const previous = project.activePlanVersionId ? tx.require("planVersions", project.activePlanVersionId) : null;\n      if ((previous?.planVersionId ?? null) !== c.supersedes) throw new AutomationStoreError("AUTOMATION_CONFLICT", "Planner candidate predecessor does not match the active PlanVersion.");\n      if (previous) tx.replace("planVersions", { ...previous, status: "SUPERSEDED" });\n      const timestamp = now();\n      const planVersion: PlanVersion = { planVersionId: c.planVersionId, projectId: c.projectId, requirementVersionId: c.requirementVersionId, version: c.version, status: "ACTIVE", createdBy: "planner-provider", origin: text(input.provider, "planner.provider", 256), requirementPayloadSha256: c.requirementPayloadSha256, planningMode: "JIT", plannerRole: "PLANNER", plannerChatRef: null, currentStageId: c.currentStageId, createdAt: timestamp, supersedes: c.supersedes };\n      tx.insert("planVersions", planVersion);\n      const stageSpecs: StageSpec[] = c.stages.map((stage) => ({ stageSpecId: stage.stageSpecId, planVersionId: planVersion.planVersionId, stageKey: stage.stageKey, name: stage.name, objective: stage.objective, dependsOn: [...stage.dependsOn], acceptanceCriteria: [...stage.acceptanceCriteria], detailLevel: stage.detailLevel, assumptions: [...stage.assumptions], risks: [...stage.risks], specVersion: stage.specVersion, status: "ACTIVE", ordinal: stage.ordinal, goal: stage.objective, createdAt: timestamp, supersedes: stage.supersedes }));\n      for (const stage of stageSpecs) tx.insert("stageSpecs", stage);\n      const stepSpecs: StepSpec[] = c.steps.map((step) => ({ stepSpecId: step.stepSpecId, stageSpecId: step.stageSpecId, stepKey: step.stepKey, specVersion: step.specVersion, kind: step.kind, ordinal: step.ordinal, objective: step.objective, inputs: [...step.inputs], expectedOutputs: [...step.expectedOutputs], acceptanceCriteria: [...step.acceptanceCriteria], assumptions: [...step.assumptions], constraints: [...step.constraints], goal: step.objective, riskClass: step.riskClass, sideEffectClass: step.sideEffectClass, specStatus: "ACTIVE", createdAt: timestamp, supersedes: step.supersedes }));\n      for (const step of stepSpecs) { tx.insert("stepSpecs", step); tx.insert("stepRuntimes", { stepRuntimeId: \`runtime:\${step.stepSpecId}\`, stepSpecId: step.stepSpecId, lifecycle: "NOT_STARTED", terminalResult: null, waitReason: "NONE", currentAttemptId: null, revision: 0, createdAt: timestamp, updatedAt: timestamp }); }\n      tx.replace("automationProjects", { ...project, activePlanVersionId: planVersion.planVersionId, updatedAt: timestamp, revision: project.revision + 1 });\n      tx.replace("actionIntents", { ...intent, state: "COMPLETED", plannerState: "PROMOTED", promotedPlanVersionId: planVersion.planVersionId });\n      tx.replace("actionAttempts", { ...attempt, externalSideEffectCertainty: "TERMINAL_CONFIRMED", plannerResultClassification: "VALID_RESULT" });\n      tx.appendAudit({ projectId: project.projectId, entityType: "PlanVersion", entityId: planVersion.planVersionId, eventType: "PLANNER_PLAN_PROMOTED", actorType: "AUTOMATION", actorRef: null, boundedPayload: { actionIntentId: intent.intentId, actionAttemptId: attempt.actionAttemptId, validationStatus: input.validationStatus, providerRequestRef: input.providerRequestRef, providerObservationRef: input.providerObservationRef }, correlationId: intent.intentId, causationId: attempt.actionAttemptId });\n      return { planVersion: clone(planVersion), stageSpecs: clone(stageSpecs), stepSpecs: clone(stepSpecs) };\n    });\n  }\n\n`;
  text = once(text, marker, method + marker, "persistValidatedPlannerCandidate method");
  return text;
});

await patch("src/automation/schema.ts", (text) => {
  text = once(text, '    optionalString(item.plannerRequestCanonical ?? null, `actionIntents[${index}].plannerRequestCanonical`, 32 * 1024);', '    optionalString(item.plannerRequestCanonical ?? null, `actionIntents[${index}].plannerRequestCanonical`, 32 * 1024);\n    optionalString(item.logicalPlannerRequestId ?? null, `actionIntents[${index}].logicalPlannerRequestId`, 256);\n    optionalString(item.plannerRequirementVersionId ?? null, `actionIntents[${index}].plannerRequirementVersionId`, 256);\n    optionalString(item.plannerRequirementPayloadSha256 ?? null, `actionIntents[${index}].plannerRequirementPayloadSha256`, 128);\n    if (item.plannerOperation !== undefined && item.plannerOperation !== null) enumValue(item.plannerOperation, `actionIntents[${index}].plannerOperation`, new Set(["PLAN_REQUIREMENT", "DETAIL_STAGE"]));\n    if (item.plannerMaxProviderAttempts !== undefined && item.plannerMaxProviderAttempts !== null) integer(item.plannerMaxProviderAttempts, `actionIntents[${index}].plannerMaxProviderAttempts`, 1, 16);\n    if (item.plannerState !== undefined && item.plannerState !== null) enumValue(item.plannerState, `actionIntents[${index}].plannerState`, new Set(["ACTIVE", "PROMOTED", "FAILED"]));\n    optionalString(item.promotedPlanVersionId ?? null, `actionIntents[${index}].promotedPlanVersionId`, 256);', "schema intent logical fields");
  text = once(text, '    if (item.plannerResultClassification !== undefined && item.plannerResultClassification !== null) enumValue(item.plannerResultClassification, `actionAttempts[${index}].plannerResultClassification`, new Set(["INVALID_OUTPUT_RETRYABLE"]));', '    optionalString(item.logicalPlannerRequestId ?? null, `actionAttempts[${index}].logicalPlannerRequestId`, 256);\n    if (item.attemptNumber !== undefined && item.attemptNumber !== null) integer(item.attemptNumber, `actionAttempts[${index}].attemptNumber`, 1, 1_000_000);\n    optionalString(item.providerTargetRef ?? null, `actionAttempts[${index}].providerTargetRef`, 2_000);\n    if (item.externalSideEffectCertainty !== undefined && item.externalSideEffectCertainty !== null) enumValue(item.externalSideEffectCertainty, `actionAttempts[${index}].externalSideEffectCertainty`, outcomeCertainties);\n    if (item.plannerResultClassification !== undefined && item.plannerResultClassification !== null) enumValue(item.plannerResultClassification, `actionAttempts[${index}].plannerResultClassification`, new Set(["INVALID_OUTPUT_RETRYABLE", "UNKNOWN_AFTER_SIDE_EFFECT", "VALID_RESULT"]));', "schema attempt logical fields");
  return text;
});

await patch("src/automation/planner-provider-integration.ts", (text) => {
  text = text.replace('candidate: normalized.candidate, actionIntentId:', 'candidate: validation.normalizedCandidate, actionIntentId:');
  return text;
});

await patch("src/automation/webgpt-policy-authority.ts", (text) => {
  text = once(text, '  authorizePinned(operation: BudgetKind, correlationId: string, policyVersionId: string, runtimeCapability: RuntimeCapability): Promise<WebGptPolicyAdmission>;', '  authorizePinned(operation: BudgetKind, correlationId: string, policyVersionId: string, runtimeCapability: RuntimeCapability, scopeProjectId?: string | null): Promise<WebGptPolicyAdmission>;', "authorizer pinned signature");
  text = once(text, '  async authorizePinned(operation: BudgetKind, correlationId: string, policyVersionId: string, runtimeCapability: RuntimeCapability): Promise<WebGptPolicyAdmission> {\n    const decision = await this.evaluatePinned(operation, correlationId, policyVersionId, runtimeCapability);', '  async authorizePinned(operation: BudgetKind, correlationId: string, policyVersionId: string, runtimeCapability: RuntimeCapability, scopeProjectId?: string | null): Promise<WebGptPolicyAdmission> {\n    const decision = await this.evaluatePinned(operation, correlationId, policyVersionId, runtimeCapability, scopeProjectId);', "authority pinned implementation");
  return text;
});

await patch("src/main/main.ts", (text) => {
  text = once(text, 'import { runStageK1DPositiveRetrySmoke, runStageK1DRealPlannerSmoke, type StageK1DProvenance } from "../automation/stage-k1-d-real-planner-smoke.ts";\nimport { runStageK1DReconcileOnly } from "../automation/stage-k1-d-reconcile-only.ts";\nimport { assessStageK1DProvenance } from "../automation/stage-k1-d-provenance.ts";', 'import { runStageK1DRealPlannerSmoke } from "../automation/stage-k1-d-real-planner-smoke.ts";\nimport { runStageK1DReconcileOnly } from "../automation/stage-k1-d-reconcile-only.ts";\nimport { assessStageK1DProvenance, type StageK1DProvenance } from "../automation/stage-k1-d-provenance.ts";', "K1D imports");
  text = once(text, '    authorizePositiveRetry: process.env.K1D_POSITIVE_RETRY_AUTHORIZED === "1",\n    provenance,\n  } as const;\n  const evidence = process.env.K1D_POSITIVE_RETRY_SMOKE === "1"\n    ? await runStageK1DPositiveRetrySmoke(smokeOptions)\n    : await runStageK1DRealPlannerSmoke(smokeOptions);', '    provenance,\n  } as const;\n  const evidence = await runStageK1DRealPlannerSmoke(smokeOptions);', "remove legacy positive retry smoke");
  text = text.replaceAll('request.plannerOperation', 'request.operation');
  return text;
});

const provenance = `export interface StageK1DProvenance {\n  source_commit: string | null;\n  worktree_state: string | null;\n  worktree_state_sha256: string | null;\n  source_tree_sha256: string | null;\n  build_timestamp: string | null;\n  package_timestamp: string | null;\n  executable_path: string | null;\n  executable_sha256: string | null;\n  expected_executable_sha256: string | null;\n  runner_script_sha256: string | null;\n  expected_runner_script_sha256: string | null;\n  evidence_timestamp: string;\n  verified: boolean;\n  verification_errors: string[];\n}\n\nfunction stringValue(value: Record<string, unknown> | null, key: string): string | null {\n  const item = value?.[key];\n  return typeof item === "string" && item.trim() ? item.trim() : null;\n}\n\nexport function assessStageK1DProvenance(input: { manifest: Record<string, unknown> | null; executablePath: string; executableSha256: string | null; runnerScriptSha256: string | null }): StageK1DProvenance {\n  const m = input.manifest;\n  const errors: string[] = [];\n  if (!m) errors.push("PACKAGE_MANIFEST_MISSING");\n  const expectedExecutable = stringValue(m, "executable_sha256");\n  const expectedRunner = stringValue(m, "runner_script_sha256");\n  if (!input.executableSha256) errors.push("EXECUTABLE_HASH_UNAVAILABLE");\n  else if (expectedExecutable && expectedExecutable !== input.executableSha256) errors.push("EXECUTABLE_HASH_MISMATCH");\n  if (expectedRunner && input.runnerScriptSha256 !== expectedRunner) errors.push("RUNNER_SCRIPT_HASH_MISMATCH");\n  return { source_commit: stringValue(m, "source_commit"), worktree_state: stringValue(m, "worktree_state"), worktree_state_sha256: stringValue(m, "worktree_state_sha256"), source_tree_sha256: stringValue(m, "source_tree_sha256"), build_timestamp: stringValue(m, "build_timestamp"), package_timestamp: stringValue(m, "package_timestamp"), executable_path: input.executablePath || null, executable_sha256: input.executableSha256, expected_executable_sha256: expectedExecutable, runner_script_sha256: input.runnerScriptSha256, expected_runner_script_sha256: expectedRunner, evidence_timestamp: new Date().toISOString(), verified: errors.length === 0, verification_errors: errors };\n}\n`;
await writeFile("src/automation/stage-k1-d-provenance.ts", provenance, "utf8");

await rm(new URL(import.meta.url), { force: true });
